namespace BattleBomb.Core.Items
{
    /// <summary>
    /// D34's two weapon classes through the vertical slice. A bow turns Light into a shot
    /// (§2.2's ranged identity); everything else fights the D19 kit. More classes are content.
    /// </summary>
    public enum WeaponClass
    {
        None = 0,
        Sword = 1,
        Bow = 2,
    }
}
