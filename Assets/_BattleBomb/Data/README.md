# Data

Authored assets only — no code (§1). ScriptableObject definitions live in `Gameplay`; the assets
authored against them live here and convert to plain Core structs via `.ToRuntime()` (§5).

This file exists so the folder survives a clone; delete it once real data lands.
